const planes = [
  { name: "fighter", numbers: [4, 15, 16, 18, 22, 35] },
  { name: "bomber", numbers: [1, 2, 18, 24, 25, 26, 29, 52] },
];

planes.forEach((number) => console.log(planes));

planes.forEach((number) => console.log(number));





planes.forEach(async (numbers) => {
   console.log(numbers);
  });

